---
title: Infrastructe As Code With JavaScript 🤖
description: Infrastructe as code with JavaScript 🤖
template: splash
pagefind: false
---

import { Card, CardGrid, LinkButton } from '@astrojs/starlight/components';

<div style="display: flex; justify-content: center; flex-flow: wrap;">
</div>

<div style="display: flex; justify-content: center; flex-flow: wrap;">
```js wrap
await retry(async () => {
  const response = await fetch(`https://${server_name}/404-uc3C6`, {
    agent: new https.Agent({
      lookup: function(hostname, options, callback) {
        callback(undefined, address, 4)
      }
    })
  })
  if (response.status !== 404) {
    throw new Error(`Service ${service.name} not responding 404 status`)
  }
})
```
</div>

<h2 style="text-align:center;">Active Projects By <a href="https://linkedin.com/in/jdecaron">JD Caron</a></h2>

<CardGrid stagger>
	<Card title="AliaJS" icon="right-arrow">
		Infrastructe as code with JavaScript 🤖

    https://github.com/jdecaron/aliajs
	</Card>
	<Card title="Rollance" icon="right-arrow" href="/sauce">
		Manufacturing company, focused on agility & 無駄.

    https://rollance.com
	</Card>
</CardGrid>

<h2 style="text-align:center;">More <span style="color:rgb(244 66 80)">Magic</span> 🪄 to Come</h2>

<div style="text-align:center;">
<LinkButton data-cal-link="jcaron/30min" data-cal-namespace="30min" data-cal-config='{"layout":"month_view"}' href="#">Let's Meet 👋️️</LinkButton>
</div>

<script type="text/javascript">{`
  (function (C, A, L) { let p = function (a, ar) { a.q.push(ar); }; let d = C.document; C.Cal = C.Cal || function () { let cal = C.Cal; let ar = arguments; if (!cal.loaded) { cal.ns = {}; cal.q = cal.q || []; d.head.appendChild(d.createElement("script")).src = A; cal.loaded = true; } if (ar[0] === L) { const api = function () { p(api, arguments); }; const namespace = ar[1]; api.q = api.q || []; if(typeof namespace === "string"){cal.ns[namespace] = cal.ns[namespace] || api;p(cal.ns[namespace], ar);p(cal, ["initNamespace", namespace]);} else p(cal, ar); return;} p(cal, ar); }; })(window, "https://app.cal.com/embed/embed.js", "init");
Cal("init", "30min", {origin:"https://cal.com"});

  Cal.ns["30min"]("ui", {"hideEventTypeDetails":false,"layout":"month_view"});
`}</script>
